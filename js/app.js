(function () {
  'use strict';

  // Declare app level module which depends on views, and components
  angular.module('monopoly', ['ui.router', 'ui.bootstrap']);
  // Declare configuration variables
  angular.module('monopoly').constant('initialAmount', 200);

}());
